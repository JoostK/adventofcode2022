pub fn run(_input: &str) -> usize {
    // input.lines().count()
    0
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn examples() {
        let example = "";

        assert_eq!(run(example), 0);
    }
}
